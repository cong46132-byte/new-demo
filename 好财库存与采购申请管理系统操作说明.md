# 好财库存与采购申请管理系统操作说明

## 1. 系统说明

本系统用于演示企业物品档案、入库、领用出库、采购申请、采购处理、库存预警及库存统计等业务。

当前版本为静态 Demo：

- 数据保存在当前浏览器的 `localStorage` 中，刷新页面后仍会保留。
- 不包含真实账号登录、服务器权限、数据库及多人实时同步。
- 页面中的角色切换用于模拟不同岗位的操作权限。
- 执行新增、审批、出入库等操作时，系统会显示加载状态和操作结果提示。

## 2. 演示角色

页面右上角可以切换角色。不同角色能够执行的业务如下：

| 角色 | 演示人员 | 主要权限 |
| --- | --- | --- |
| 仓库管理员 | 林知远 | 维护物品档案、维护基础字典、登记入库、审核领用、执行出库、登记退回、查看库存及预警 |
| 普通员工 | 陈小雨（产品部）、苏晴（行政部） | 发起领用申请、新建采购申请、保存草稿、提交本人申请、查看申请进度 |
| 部门负责人 | 周明 / 赵岚 | 审批或驳回本部门采购申请；不同负责人对应不同部门 |
| 财务 / 采购人员 | 许安 | 维护采购信息、选择供应商、登记部分或全部到货、查看关联入库记录 |

当当前角色没有操作权限时，按钮会禁用或提示需要切换到相应角色。

## 3. 推荐完整演示流程

### 3.1 采购业务闭环

```text
普通员工新建采购申请
        ↓
保存草稿 / 编辑申请
        ↓
提交审批
        ↓
本部门负责人审批通过或驳回
        ↓
审批通过后自动生成采购任务
        ↓
财务 / 采购人员填写供应商、单价和预计到货日期
        ↓
登记部分到货或全部到货
        ↓
系统自动生成入库记录并增加库存
        ↓
全部到货后采购任务及采购申请自动完成
```

### 3.2 领用出库闭环

```text
普通员工发起领用申请
        ↓
仓库管理员审核
        ↓
审核通过后锁定申请数量
        ↓
仓库管理员执行出库
        ↓
系统扣减当前库存并生成出库流水
        ↓
如需退回，可从已完成领用单登记退货入库
```

### 3.3 库存预警处理闭环

```text
系统根据库存和批次效期自动生成预警
        ↓
在预警页面查看缺货、低库存或效期异常
        ↓
从预警直接创建采购申请
        ↓
提交、审批、采购并登记到货
        ↓
库存恢复后缺货或低库存预警自动消失
```

## 4. 各功能操作说明

### 4.1 工作台总览

进入“总览”后，可以查看：

- 在库物品数量、库存金额、当前角色待办数量和库存预警数量。
- 当前库存预警、最近库存动态和当前角色待办。
- 常用业务入口。

点击统计卡片、待办事项或“查看全部”等入口，可以进入对应业务页面。

### 4.2 物品档案

#### 新增物品

1. 切换为“仓库管理员”。
2. 进入“物品档案”。
3. 点击“新增物品”。
4. 填写物品名称、编码、条形码、分类、规格、单位、库存位置和安全库存。
5. 点击保存。

物品编码用于业务识别，条形码可用于后续扫码场景。分类、单位和库存位置从基础字典中选择。

#### 编辑或停用物品

1. 在物品列表找到目标物品。
2. 点击“编辑”修改档案信息，或点击“停用”。
3. 停用后的物品不再出现在新建采购、领用和普通入库的可选列表中。

当前库存不能在物品编辑表单中直接修改，应通过入库、出库或库存调整产生变动记录。

#### 查看库存构成

- 当前库存：已完成出入库后的实际数量。
- 锁定库存：领用申请已审核，但尚未实际出库的数量。
- 可用库存：当前库存减去锁定库存。
- 点击“批次”可以查看批次号、剩余数量、供应商、入库日期和效期状态。

#### 维护基础字典

1. 点击页面右上方“基础字典”。
2. 分别维护物品分类、计量单位和库存位置。
3. 输入新选项后添加，或删除未使用的选项。
4. 点击“保存字典”。

正在被物品档案使用的字典值不应随意删除。

### 4.3 入库管理

#### 新增独立入库

1. 切换为“仓库管理员”。
2. 进入“入库管理”，点击“新增入库”。
3. 选择入库类型：采购入库、退货入库或库存调整。
4. 选择物品并填写数量、单价、供应商、批次及相关原因。
5. 如物品需要效期管理，可填写有效期。
6. 提交后，系统增加库存并生成入库流水。

业务规则：

- 采购入库必须填写有效供应商、数量、单价和批次。
- 供应商支持搜索；输入新名称并确认后，可在后续表单中继续使用。
- 退货入库必须关联已完成且仍有可退数量的领用单。
- 库存调整可调增或调减，调减数量不能超过可用库存。
- 采购申请产生的到货应优先在“采购处理”中登记，便于保持申请、采购任务和入库记录的关联。

### 4.4 领用出库

#### 发起领用申请

1. 切换为“普通员工”。
2. 进入“领用出库”，点击“发起领用申请”。
3. 选择物品，填写领用数量、部门和用途。
4. 提交后状态变为“待审核”。

系统会校验物品是否启用以及申请数量是否超过可用库存。

#### 审核并出库

1. 切换为“仓库管理员”。
2. 对待审核申请选择“审核通过”或“驳回”。
3. 审核通过后，申请数量计入锁定库存。
4. 点击“执行出库”，系统扣减当前库存并生成领用出库流水。

#### 登记退回

1. 找到状态为“已出库”的领用单。
2. 点击“登记退回”。
3. 填写本次退回数量并确认。
4. 系统增加库存并生成退货入库记录。

累计退回数量不能超过原领用数量。

### 4.5 采购申请

#### 新建并提交申请

1. 切换为“普通员工”。
2. 进入“采购申请”，点击“新建采购申请”。
3. 选择采购物品并填写数量；可点击“添加物品”批量申请多个物品。
4. 选择申请部门，填写采购用途和期望到货日期。
5. 可先保存为草稿，也可完成后提交审批。

草稿和被驳回的申请可以继续编辑并重新提交。

#### 审批申请

1. 根据申请部门切换到对应“部门负责人”。
2. 在待审批申请中点击“详情”，核对申请人、部门、用途、物品和数量。
3. 点击“审批通过”或“驳回”。
4. 驳回时填写驳回原因。

审批通过后，系统按采购明细自动生成采购任务。申请详情中会保留创建、提交、审批、驳回等操作时间轴。

审批通过的申请会继续显示“待采购、采购中、部分到货、已完成”等业务进度，并可通过列表中的“采购处理”进入后续采购任务。供应商、采购价和预计到货信息由财务 / 采购人员维护，不由申请人填写。

采购申请状态为：

```text
草稿 → 待审批 → 待采购 → 采购中 → 部分到货 → 已完成
             ↘ 已驳回 → 编辑后重新提交
```

采购申请仅在“草稿”或“已驳回”状态下允许申请人编辑。提交后申请内容锁定；审批通过后如需调整供应商、价格或到货安排，应在采购任务中处理，不能回改原申请。一个申请包含多项物品时，会生成多条采购任务，全部任务完成后申请才自动变为“已完成”。

### 4.6 采购处理

#### 填写采购信息

1. 切换为“财务 / 采购人员”。
2. 进入“采购处理”。
3. 待采购任务点击“完善采购”；进入采购中且尚未到货时，按钮显示为“调整采购”。
4. 选择或新增供应商，填写采购单价和预计到货日期。
5. 将采购状态设为“采购中”并保存。供应商、单价、预计到货日期或采购状态未完善时，系统不允许登记到货。

采购任务编辑规则：

| 任务状态 | 允许操作 |
| --- | --- |
| 待采购 | 完善供应商、采购单价、预计到货日期并确认采购 |
| 采购中且尚未到货 | 允许调整供应商、价格和预计到货日期，采购状态不可退回；也可登记到货 |
| 部分到货 | 供应商与采购单价锁定，只能继续登记剩余到货 |
| 已完成 | 不可编辑，只能查看关联入库记录 |

#### 登记到货

1. 点击采购任务的“登记到货”。
2. 核对只读显示的供应商、采购单价和预计到货日期，填写本次到货数量、到货批次及有效期。
3. 确认后，系统只增加本次实际到货数量，并生成一条独立入库记录。
4. 若本次到货少于采购数量，任务状态变为“部分到货”，后续可继续登记。
5. 累计到货数量达到采购数量后，任务自动完成。

点击已完成任务的“入库记录”，可以查看该任务产生的全部分批入库记录。

### 4.7 库存预警

系统自动识别以下预警：

| 预警类型 | 判断规则 |
| --- | --- |
| 缺货 | 当前库存等于 0 |
| 低库存 | 当前库存大于 0，但低于安全库存 |
| 即将过期 | 有库存批次将在预警周期内到期 |
| 已过期 | 有库存批次的有效期早于当前日期 |

操作方式：

1. 进入“库存预警”。
2. 按预警类型筛选异常物品。
3. 查看当前库存、安全库存、批次效期和处理建议。
4. 对缺货或低库存物品点击“创建采购申请”。
5. 如果该物品已经存在草稿、待审批或采购中的业务，页面会显示当前采购进度，避免重复申请。

库存补足后，缺货和低库存预警会自动更新；异常批次耗尽后，对应效期预警会消失。

### 4.8 库存记录与统计

#### 库存流水

1. 进入“库存记录与统计”，选择“库存流水”。
2. 可按物品名称或编码、部门、物品分类、操作类型和日期范围筛选。
3. 点击“搜索”应用条件，点击“重置”清空条件。
4. 页面汇总筛选范围内的入库数量和出库数量，同时展示当前库存及库存金额。
5. 点击右上方“导出库存流水”，导出当前筛选结果。

#### 领用明细

1. 切换到“领用明细”页签。
2. 可按领用人、部门及日期范围筛选。
3. 点击“导出领用明细”导出当前结果。

列表默认每页展示 10 条，表格随页面内容自然展开。

## 5. 通用操作

### 5.1 搜索与重置

- 输入或选择筛选条件后，点击“搜索”才会应用条件。
- 点击“重置”会清空当前页面的筛选条件。
- 搜索框支持物品名称、编码等页面提示的字段。

### 5.2 分页与表格

- 业务列表默认每页显示 10 条。
- 数据超过 10 条时才显示分页，并可使用表格底部按钮切换数据。
- 表格随页面自然展开，通过分页控制单页数据量。

### 5.3 外观设置

点击右上角“外观”，可以切换：

- 浅色或深色模式。
- 标准蓝、紫罗兰或青蓝主题色。
- 舒适或紧凑显示密度。

外观设置会保存在当前浏览器中。

### 5.4 全局搜索

使用顶部搜索框可以快速查找物品、采购申请和采购任务，也可以直接进入常用功能页面。

### 5.5 恢复演示数据

如需重新演示完整流程，可点击页面右上角的恢复按钮，并在确认弹窗中确认。恢复后，当前浏览器内新增或修改的演示数据将被初始数据替换。

## 6. 关键业务规则

1. 当前库存由已完成的入库和出库记录自动计算，不能在物品档案中直接填写。
2. 可用库存等于当前库存减去锁定库存。
3. 领用申请数量不能超过可用库存。
4. 停用物品不能继续用于新的采购、领用或普通入库业务。
5. 采购申请只有审批通过后才会生成采购任务。
6. 分批到货只增加本次实际到货数量，不能超过任务剩余未到货数量。
7. 出库时优先扣减有效期更近的库存批次。
8. 每次库存变化都会记录单号、时间、物品、数量、操作人、部门和变化原因。
9. 库存金额按当前库存和物品最新入库单价估算。

## 7. 建议客户演示脚本

1. 以仓库管理员身份打开工作台，介绍库存、金额、待办和预警。
2. 进入物品档案，展示物品字段、可用/锁定库存、批次和基础字典。
3. 切换普通员工，创建一张包含多个物品的采购申请并提交。
4. 切换对应部门负责人，查看申请详情及操作时间轴并审批通过。
5. 切换财务 / 采购人员，填写供应商和采购信息，分两次登记到货。
6. 返回物品档案，展示库存随到货自动增加，并查看批次库存。
7. 创建领用申请，切换仓库管理员审核并执行出库，展示库存减少。
8. 打开库存预警，展示预警关联采购状态及库存恢复后的自动更新。
9. 打开库存记录，演示多条件筛选、分页及数据导出。
10. 最后展示全局搜索、主题颜色、深浅模式和紧凑布局。
